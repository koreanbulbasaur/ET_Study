use world;

describe city;

select id, name
	from city;
    
select id, name, countrycode, district, population
	from city;
    
select *
	from city;
    
select district, countrycode, name, id
	from city;